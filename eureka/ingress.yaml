---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: eureka
  labels:
    run: eureka
spec:
  rules:
  - host: eureka
    http:
      paths:
      - backend:
          serviceName: eureka
          servicePort: 8080
